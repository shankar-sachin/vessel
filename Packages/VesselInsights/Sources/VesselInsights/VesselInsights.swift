import Foundation

/// Placeholder so the module exists while its implementation is built out in a
/// later phase. Replaced, not added to.
public enum VesselInsightsModule {
    public static let name = "VesselInsights"
}
